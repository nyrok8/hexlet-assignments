<h1>Articles</h1>
<ul>
    <% @articles.each do |article| %>
        <li>
            <%= "#{article.title}" %>
            <p>Подробнее про <%= link_to "#{article.title}", article_path(article) %></p>
        </li>
    <% end %>
</ul>
